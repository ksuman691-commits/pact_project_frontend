'use client';

import React from 'react';

interface WalletSummaryProps {
  balance?: number;
  locked?: number;
  rewards?: number;
  loading?: boolean;
}

export default function WalletSummary() {
  return null;
}
