==== PASTE THIS ENTIRE TEXT INTO YOUR LLM (Claude, GPT, Deepseek, etc.) IN VS CODE ====

I have a Next.js 14 CirclePact app with a fully-built frontend but dummy data. Backend APIs exist but aren't connected. I need to fix 10 critical backend-alignment issues identified in code review. 

PROJECT CONTEXT:
- Frontend: src/components/CirclePactApp.tsx (main dashboard)
- Types: src/types/index.ts
- API Layer: src/services/api.ts (axios instance, already configured)
- Auth Store: src/store/auth.ts
- Current Status: All UI shows hardcoded/dummy data, no real API calls

BACKEND ENDPOINTS AVAILABLE:
- Pacts: GET /pacts, POST /pacts, GET /pacts/{id}, POST /pacts/{id}/proof, GET /pacts/{id}/stats
- Verifications: POST /verifications, GET /verifications/{id}
- Shorts: GET /pacts/{id}/shorts, GET /shorts
- Circles: GET /circles, POST /circles, GET /circles/{id}/join-requests
- Join Requests: POST /join-requests/{id}/approve, POST /join-requests/{id}/reject
- Users: GET /users/me/stats, GET /users/me/wallet

THE 10 ISSUES (FIX IN THIS ORDER):

==== ISSUE #1: MOCK DATA STRUCTURE MISMATCH ====
Files: src/types/index.ts, src/components/CirclePactApp.tsx, src/services/api.ts

Problem: 
- UI shows: supportPool, believers, doubters, confidence (don't exist in backend)
- Backend has: Pact model with believers_count fetched from Verification table
- Confidence is: average verification.confidence_score

Fix:
1. Update Pact interface in types/index.ts to include:
   - believers_count: number
   - confidence_score: number (0-100)
   - verification_count: number
   
2. Add to api.ts:
   export const getPactStats = async (pactId: string) => {
     return api.get(`/pacts/${pactId}/stats`);
   };

3. In CirclePactApp.tsx, replace dummy pact initialization with API call:
   useEffect(() => {
     pacts.forEach(pact => {
       getPactStats(pact.id).then(response => {
         // Update pact with real stats
       });
     });
   }, []);

Test: Network tab should show GET /pacts/{id}/stats returning { believers_count, confidence_score, verification_count }

==== ISSUE #2: VOTE UI ≠ VERIFICATION FLOW ====
Files: src/components/CirclePactApp.tsx, src/services/api.ts

Problem:
- Current: Clicking believe/doubt does nothing, just toggles local state
- Backend: Requires 4-question verification form with LLM scoring
- Result: Confidence score calculated from answers

Fix:
1. Replace the handleVote function to open a modal instead:
   const handleVote = (pactId: number) => {
     setShowVerificationModal(true);
     setSelectedPactId(pactId);
   };

2. Create VerificationForm component with 4 text inputs for questions
   
3. On form submit, call new api.ts function:
   export const submitVerification = async (pactId: string, answers: {
     answer_1: string;
     answer_2: string;
     answer_3: string;
     answer_4: string;
   }) => {
     return api.post(`/verifications`, { pact_id: pactId, ...answers });
   };

4. Handle response which includes confidence_score, update pact stats

Test: Click believe button → 4-question modal opens → submit → API POST to /verifications → get back confidence score

==== ISSUE #3: PROOF CLIPS STRUCTURE ====
Files: src/types/index.ts, src/components/CirclePactApp.tsx, src/services/api.ts

Problem:
- Frontend expects: proofClips[] = [{day, type, text}, ...]
- Backend stores: Single proof_url on Pact, separate Shorts table for videos
- Need: Proper mapping between UI and backend data model

Fix:
1. Add Short interface to types/index.ts:
   export interface Short {
     id: string;
     pact_id: string;
     video_url: string;
     created_at: string;
   }

2. Add to api.ts:
   export const uploadProof = async (pactId: string, file: File) => {
     const formData = new FormData();
     formData.append('file', file);
     return api.post(`/pacts/${pactId}/proof`, formData, {
       headers: { 'Content-Type': 'multipart/form-data' }
     });
   };

   export const getShorts = async (pactId: string) => {
     return api.get(`/pacts/${pactId}/shorts`);
   };

3. In CirclePactApp.tsx, fetch shorts on pact load and render video grid

Test: Upload file → see network POST /pacts/{id}/proof → then GET /pacts/{id}/shorts returns array with your video

==== ISSUE #4: NO ROLE-BASED ACCESS CONTROL ====
Files: src/components/CirclePactApp.tsx, src/services/api.ts

Problem:
- Anyone can click upload/verify buttons
- Backend enforces: only creator can upload, only verified users can verify
- Need: Frontend permission checks

Fix:
1. Check if current user is pact creator before showing upload button:
   const isCreator = currentUser.id === pact.creator_id;
   
   <button 
     disabled={!isCreator}
     className={isCreator ? 'bg-blue-500' : 'bg-gray-300 cursor-not-allowed'}
   >
     {isCreator ? 'Upload Proof' : 'Creator only'}
   </button>

2. Add global error handler in api.ts for 403 responses:
   api.interceptors.response.use(
     response => response,
     error => {
       if (error.response?.status === 403) {
         console.log('[v0] Permission denied');
         toast.error('You do not have permission');
       }
       return Promise.reject(error);
     }
   );

Test: Log in as creator → button enabled. Log in as different user → button disabled.

==== ISSUE #5: REPUTATION/STREAK HARDCODED ====
Files: src/components/CirclePactApp.tsx, src/services/api.ts

Problem:
- Dashboard shows hardcoded values: streak = 14, reputation = 92
- Should fetch from: /users/me/stats endpoint

Fix:
1. Add to api.ts:
   export const getUserStats = async () => {
     return api.get('/users/me/stats');
   };

2. In CirclePactApp.tsx component mount:
   useEffect(() => {
     getUserStats().then(response => {
       setUserStats({
         streak: response.data.current_streak,
         reputation: response.data.reputation_score,
         longestStreak: response.data.longest_streak
       });
     });
   }, []);

3. Replace hardcoded values in JSX:
   <span className="text-4xl font-black">{userStats.streak}</span>

Test: Refresh page → streak and reputation update from API, not hardcoded

==== ISSUE #6: COMMENTS SYSTEM NOT IMPLEMENTED ====
Action: BEFORE YOU CODE - Ask your backend team:
"Do we have a comments/discussion API endpoint yet?"

If YES: Get endpoint docs and implement similar to verification flow
If NO: Comment out the comment UI section in CirclePactApp.tsx for MVP

Test: Backend confirms it exists and working → integrate. Otherwise, skip.

==== ISSUE #7: JOIN REQUESTS HAVE NO UI ====
Files: src/components/CirclePactApp.tsx, src/services/api.ts

Problem:
- Backend has full workflow: GET join-requests, approve, reject
- Frontend: No UI to show or manage requests

Fix:
1. Add to api.ts:
   export const getJoinRequests = async (circleId: string) => {
     return api.get(`/circles/${circleId}/join-requests`);
   };

   export const approveJoinRequest = async (requestId: string) => {
     return api.post(`/join-requests/${requestId}/approve`);
   };

   export const rejectJoinRequest = async (requestId: string) => {
     return api.post(`/join-requests/${requestId}/reject`);
   };

2. Create new component: src/components/JoinRequestsModal.tsx
   - Shows pending requests
   - Approve/Reject buttons call API
   - Closes modal after action

3. In Circles tab of CirclePactApp.tsx, add button to open modal:
   <button onClick={() => setShowJoinRequests(true)}>
     Pending Requests ({pendingCount})
   </button>

Test: Create circle as User A, request join as User B, approve/reject as User A → User B added or rejected

==== ISSUE #8: CATEGORY SYSTEM UNDEFINED ====
Files: src/types/index.ts, src/components/CirclePactApp.tsx

Problem:
- Frontend uses freetext input for category
- Backend likely has predefined categories

Fix:
1. Add enum to types/index.ts:
   export enum PactCategory {
     FITNESS = 'fitness',
     LEARNING = 'learning',
     FINANCE = 'finance',
     PRODUCTIVITY = 'productivity',
     HEALTH = 'health'
   }

2. Replace text input with dropdown in CirclePactApp.tsx:
   <select value={category} onChange={(e) => setCategory(e.target.value)}>
     <option value="">Select category</option>
     {Object.values(PactCategory).map(cat => (
       <option key={cat} value={cat}>{cat}</option>
     ))}
   </select>

Test: Category input is now dropdown with predefined options

==== ISSUE #9: WALLET INTEGRATION ====
Action: BEFORE YOU CODE - Ask backend:
"Do pacts require stakes? Do users have wallets?"

If YES:
1. Add to api.ts:
   export const getWallet = async () => {
     return api.get('/users/me/wallet');
   };

2. In CirclePactApp.tsx, check balance before creating pact:
   if (walletBalance < minimumStake) {
     return <p>Insufficient balance</p>;
   }

If NO: Skip this for MVP

Test: Balance displays, can't create pact with insufficient funds

==== ISSUE #10: SHORTS FEED NOT WIRED ====
Files: src/components/CirclePactApp.tsx, src/services/api.ts

Problem:
- Leaderboard tab is empty
- Backend has GET /shorts endpoint with recent videos

Fix:
1. Add to api.ts:
   export const getShortsFeed = async (limit = 20) => {
     return api.get('/shorts', { params: { limit } });
   };

2. In CirclePactApp.tsx, populate leaderboard tab:
   if (currentTab === 'leaderboard') {
     useEffect(() => {
       getShortsFeed().then(response => setShorts(response.data));
     }, []);
     
     return (
       <div className="grid grid-cols-2 gap-4">
         {shorts.map(short => (
           <video key={short.id} src={short.video_url} className="w-full h-32 object-cover rounded" />
         ))}
       </div>
     );
   }

Test: Leaderboard tab shows grid of recent proof videos

==== EXECUTION ROADMAP ====

PHASE 1 (Day 1 - 4-6 hours): Issues #1, #2, #4
- Fix pact stats, verification flow, permission checks
- Most critical for core functionality

PHASE 2 (Day 2 - 4-6 hours): Issues #3, #5, #7
- Proof uploads, user stats, join requests
- Completes main workflows

PHASE 3 (Later - as needed): Issues #6, #8, #9, #10
- Comments, categories, wallet, leaderboard
- Nice-to-have features

==== TESTING CHECKLIST ====
[ ] Open browser DevTools → Network tab
[ ] Perform each action and verify API call shows in Network tab
[ ] Check response has expected data structure
[ ] Verify UI updates with real data (not dummy data)
[ ] Test error scenarios (network error, 403 permission, 404 not found)
[ ] Remove all console.log('[v0]...') debug statements before final commit

==== FILES TO MODIFY ====
Primary: src/components/CirclePactApp.tsx
Secondary: src/types/index.ts, src/services/api.ts, src/store/auth.ts
Create: src/components/VerificationForm.tsx, src/components/JoinRequestsModal.tsx

==== BEFORE YOU START ====
1. Verify backend base URL in .env (should already be in axios instance)
2. Ask backend team the 10 questions listed in VSCODE_MASTER_PROMPT.md
3. Test one backend endpoint with Postman first to confirm it works
4. Make sure you have test user credentials

Good luck! Start with Phase 1. You've got this.
