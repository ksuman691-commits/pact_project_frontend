import CuratedLibrary from '@/components/CuratedLibrary';

export const metadata = {
  title: 'Curated Pacts | CirclePact',
  description: 'Ready-made Pacts and Dares for your Circle.',
};

export default function CuratedPactsPage() {
  return <CuratedLibrary />;
}
